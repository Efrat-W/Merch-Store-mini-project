
namespace DO;

public enum category { Prints, Apparel, Stickers, Notebooks, Mugs };
public enum menu
{
    Exit, Product, Order, OrderItem
}

public enum options
{
    Add, ShowById, ShowList, Update, DeleteFromList
}
